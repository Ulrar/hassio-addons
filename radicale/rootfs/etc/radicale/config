[auth]
type = htpasswd
htpasswd_filename = /share/radicale/users
htpasswd_encryption = md5

[server]
hosts = 0.0.0.0:5232

[rights]
type = authenticated 

[storage]
filesystem_folder = /share/radicale/

[logging]
config = /etc/radicale/logging
